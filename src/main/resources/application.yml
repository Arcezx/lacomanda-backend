server:
  port: 8090

spring:
  application:
    name: lacomanda-backend

  datasource:
    url: jdbc:mysql://localhost:3306/lacomanda?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: lacomanda_app
    password: 130723
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB